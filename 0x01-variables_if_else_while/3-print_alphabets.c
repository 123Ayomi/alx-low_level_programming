#include <stdio.h>

/**
* main - Print the alphabe
* 
* Return: Always 0 (Success)

*/
int main(void)
{
int lowerCase = 'a';
int upperCase = 'A';
while (lowercase <= 'z')
{
putchar(lowerCase);
lowerCase += 1;
}
while (upperCase <= 'z')
{
putcher(upperCase)
upperCase += 1;	
}
putchar('\n');
return (0);
}
